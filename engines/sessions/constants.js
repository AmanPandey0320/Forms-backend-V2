/**
 * @session_duration in seconds
 * @session_increment in seconds
 *
 */
const SESSION_DURATION = 172800000;
const SESSION_INCREMENT = 86400000;

module.exports = { SESSION_DURATION, SESSION_INCREMENT };
